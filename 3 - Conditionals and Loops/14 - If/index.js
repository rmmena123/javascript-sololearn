var month = readLine();

if(month == "August") {
    console.log("vacation");
}

// -----------------------------

function main() {
    var day = readLine();
    var rules = 'Obey the rules';

    if(day == "Sunday") {
        console.log(rules);
    }
}